# cache files
__pycache__/
.pytest_cache/
# vs code files
.vscode/
website/.vs/

#Terraform files
*.hcl
# but the provider lock must be committed, so every machine resolves the
# same provider versions
!.terraform.lock.hcl
*.zip
*.tfstate
*.tfstate.*
*.backup
.terraform/

# Lambda Function Files
src/lambdaFunctions.egg-info/
.github
website/index.html

# Working notes for Claude Code - deliberately local, never published.
CLAUDE.md
